function u16(n){return new Uint8Array([(n>>8)&255,n&255])}function u32(n){return new Uint8Array([(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255])}function concat(...a){let l=a.reduce((s,x)=>s+x.length,0),o=new Uint8Array(l),p=0;for(const x of a){o.set(x,p);p+=x.length}return o}function exportTTF(){const p=currentProject||defaultProject();const header=new Uint8Array([0,1,0,0]);const name=new TextEncoder().encode(p.name||"FontStudio");const data=concat(header,u16(1000),u16(name.length),name);download((p.name||"Font")+".ttf",new Blob([data],{type:"font/ttf"}))}function checks(){const p=currentProject||defaultProject(),items=[["Font name",!!p.name],["Glyph data",Object.keys(p.glyphs||{}).length>0],["Units Per Em",p.unitsPerEm>=16],["Metrics",Number.isFinite(p.ascender)&&Number.isFinite(p.descender)]];document.getElementById("checks").innerHTML=items.map(x=>`<div class="check"><span>${x[0]}</span><strong class="${x[1]?"ok":""}">${x[1]?"OK":"CHECK"}</strong></div>`).join("")}checks();